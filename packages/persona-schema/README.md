# Persona Schema

Schema placeholders for DayPilot-native personas and HomePilot `.hpersona` imports.

The first schema in this package validates the HomePilot `.hpersona` manifest before DayPilot maps the persona into a governed professional agent package.
