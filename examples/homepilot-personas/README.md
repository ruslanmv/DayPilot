# HomePilot Persona Examples

This directory may include a sample HomePilot `.hpersona` package extracted from the uploaded HomePilot archive for import testing.

Run:

```bash
python scripts/import_homepilot_persona.py examples/homepilot-personas/atlas.hpersona --preview
```
