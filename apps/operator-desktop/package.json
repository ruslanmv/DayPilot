{
  "name": "@daypilot/operator-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "echo 'operator-desktop is built via the release-desktop workflow (pnpm run build:tauri, requires the Rust toolchain)'",
    "build:tauri": "tauri build",
    "typecheck": "echo 'no TypeScript sources to typecheck in operator-desktop'",
    "lint": "echo placeholder lint",
    "test": "echo placeholder test"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/cli": "^2.1.0"
  }
}
